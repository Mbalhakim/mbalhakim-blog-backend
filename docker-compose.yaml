services:
  strapi:
    build: .
    image: strapi-blog
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - /srv/strapi/uploads:/app/public/uploads
