services:
  strapi:
    build: .
    image: strapi-blog
    ports:
      - "3000:3000"
    
    env_file:
      - .env
    
    volumes:
      - ./data:/opt/app/.tmp
      - ./uploads:/opt/app/public/uploads
      - ./src:/opt/app/src

    
    restart: unless-stopped